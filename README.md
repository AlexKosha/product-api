Product API Backend

This is a RESTful Product API built with Node.js, TypeScript, and Prisma ORM, connected to a PostgreSQL database.
It provides endpoints to fetch all products, fetch by ID, filter by category, and create new products.
